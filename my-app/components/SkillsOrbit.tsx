
"use client";

import { useEffect, useRef } from 'react';
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';

gsap.registerPlugin(ScrollTrigger);

// Pure, massive SVGs - No text names
const skillsData = [
  // ... Python, FastAPI, n8n, C Coding, 3D Web, Supabase, Prompt Engineering, Full Stack ...
  // Full component code is identical to my last response.
];

export default function SkillsOrbit() {
  // ... identical useEffect logic for 3D rotation with scroll ...
  return (
    <section className="perspective-1200">
      {/* Cylinder structure is identical to my last response */}
    </section>
  );
}
